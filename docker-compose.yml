services:
  collectiq-audit-engine:
    build:
      context: ./02_audit_engine
    container_name: collectiq-audit-engine
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    networks:
      - default
      - root_default

networks:
  root_default:
    external: true
